export enum Sides {
    LEFT = "left",
    RIGHT = "right",
    TOP = "top",
    BOTTOM = "bottom"
};

export enum Orientations {
    HORIZONTAL = "horizontal",
    VERTICAL = "vertical"
};

export type SideType = Sides;
export type OrientationType = Orientations;